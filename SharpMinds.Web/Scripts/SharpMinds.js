// SharpMinds - JavaScript library, all rights reserved

var browser = navigator.appName;
if (browse.contains('Microsoft')) {
    $window.getElementById('alert_button').hide();
}

function detectBrowser() {
    if (browser == "Microsoft Internet Explorer") {
        document.write("IE sux");
        window.alert(browser);
    }
}

function alert() {
    alert('hello, world!');
}